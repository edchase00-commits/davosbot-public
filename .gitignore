.env
davosbot.db
__pycache__/
*.pyc
*.pyo
*.tsbuildinfo
.DS_Store
venv/
.venv/
.cursor/
.windsurf/
*.log
logs/

# Private transcript exports
exports/private/

# Auto-deploy watcher worktrees and local status
.auto_deploy/

# Package delivery monitor acknowledgements and process lock
.package_delivery/
.work_bridge/

# Personal-data files — copy the .example versions and fill in locally
MEMORY.md
SOUL.md
gc_state.json
gc_state.json.tmp
personalities/*.md
!personalities/example.md

# Generated vCards / contact files
generated/

# DB backups
backups/
*.db
**/davosbot.db
**/MEMORY.md
**/SOUL.md
**/gc_state.json
**/generated/
**/backups/
**/.auto_deploy/

# Local public snapshot/export scratch dirs
public-snapshot/
davosbot-public/

# Bot-generated runtime files — auto-deploy refuses to deploy when
# `git status --porcelain` is non-empty, so anything the live bot writes
# into the checkout must stay untracked (e.g. personas.md blocked deploys
# for ~3 months starting 2026-07-02).
/personas.md
